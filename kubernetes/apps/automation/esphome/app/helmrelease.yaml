---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: esphome
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: esphome
  values:
    controllers:
      main:
        strategy: Recreate
        containers:
          main:
            image:
              repository: esphome/esphome
              tag: "2026.9.0@sha256:9959730a04c7ee38d61805681778ba5c41aa9c5d72b1ed1e910a83fa572fe18b"
            env:
              TZ: Europe/Amsterdam
              ESPHOME_DEFAULT_COMPILE_PROCESS_LIMIT: "4"
            resources:
              requests:
                cpu: 10m
                memory: 256Mi
              limits:
                memory: 2Gi
    service:
      main:
        controller: main
        ports:
          http:
            port: 6052
    persistence:
      config:
        existingClaim: esphome
        globalMounts:
          - path: /config
    route:
      main:
        parentRefs:
          - name: envoy-internal
            namespace: network
        hostnames:
          - esphome.bo0tzz.me
        rules:
          - backendRefs:
              - identifier: main
                port: 6052
