---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: alertmanager-to-github
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: alertmanager-to-github
    namespace: monitoring
  values:
    controllers:
      main:
        containers:
          main:
            image:
              # Temporary fork carrying the `is:issue` search fix (pfnet-research#64).
              # Revert to ghcr.io/pfnet-research/alertmanager-to-github once that's released.
              repository: ghcr.io/bo0tzz/alertmanager-to-github
              tag: v0.2.5-isissue2@sha256:ef2afa7a48d07a2012d88c854610823492900c4d99f89c9153b4bd70ea5cf3cd
            # Target owner/repo come from the Alertmanager webhook URL query
            # (?owner=bo0tzz&repo=clusterfuck), so they're per-request, not baked in here.
            args:
              - start
              - --labels
              - auto-triage
              - --title-template-file
              - /templates/title.tmpl
              - --body-template-file
              - /templates/body.tmpl
            envFrom:
              - secretRef:
                  name: alertmanager-to-github-secret
            probes:
              liveness:
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /metrics
                    port: 8080
                  initialDelaySeconds: 10
                  periodSeconds: 30
              readiness:
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /metrics
                    port: 8080
                  initialDelaySeconds: 5
                  periodSeconds: 15
            securityContext:
              runAsNonRoot: true
              allowPrivilegeEscalation: false
              capabilities:
                drop: ["ALL"]
            resources:
              requests:
                cpu: 10m
                memory: 32Mi
              limits:
                memory: 64Mi
    defaultPodOptions:
      priorityClassName: infra
      securityContext:
        seccompProfile:
          type: RuntimeDefault
    service:
      main:
        controller: main
        ports:
          http:
            port: 8080
    persistence:
      templates:
        type: configMap
        name: alertmanager-to-github-templates
        globalMounts:
          - path: /templates
