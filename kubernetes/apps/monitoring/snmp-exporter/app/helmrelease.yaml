---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: snmp-exporter
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: prometheus-snmp-exporter
  values:
    fullnameOverride: snmp-exporter
    resources:
      requests:
        cpu: 10m
        memory: 30Mi
      limits:
        memory: 100Mi
    serviceMonitor:
      enabled: true
      interval: 30s
      scrapeTimeout: 10s
      targets:
        - name: apc-ups
          target: 10.0.0.3
          module: [apcups]
