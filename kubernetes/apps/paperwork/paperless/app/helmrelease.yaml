---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: paperless
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: paperless
  values:
    controllers:
      main:
        containers:
          main:
            image:
              repository: ghcr.io/paperless-ngx/paperless-ngx
              tag: "3.2.0@sha256:22dc423ff48ac1629977dbf0c9625ba9f60d3bd1291a2ff173c65351984a14c2"
            env:
              PAPERLESS_URL: https://paperless.bo0tzz.me
              PAPERLESS_TIME_ZONE: "Europe/Amsterdam"
              PAPERLESS_APPS: allauth.socialaccount.providers.openid_connect
              PAPERLESS_ACCOUNT_EMAIL_VERIFICATION: none
              PAPERLESS_SOCIAL_AUTO_SIGNUP: "true"
              PAPERLESS_OCR_USER_ARGS: '{"invalidate_digital_signatures": true}'
              PAPERLESS_DBENGINE: postgresql
              PAPERLESS_DBHOST:
                valueFrom:
                  secretKeyRef:
                    name: paperless-app
                    key: host
              PAPERLESS_DBNAME:
                valueFrom:
                  secretKeyRef:
                    name: paperless-app
                    key: dbname
              PAPERLESS_DBUSER:
                valueFrom:
                  secretKeyRef:
                    name: paperless-app
                    key: username
              PAPERLESS_DBPASS:
                valueFrom:
                  secretKeyRef:
                    name: paperless-app
                    key: password
              PAPERLESS_CONSUMPTION_DIR: /data/incoming
              # Polling, not inotify: scanservjs writes to /data/incoming/scans
              # over CephFS from another node, which doesn't deliver inotify.
              PAPERLESS_CONSUMER_RECURSIVE: "true"
              PAPERLESS_CONSUMER_POLLING_INTERVAL: "60"
              PAPERLESS_DATA_DIR: /data/data
              PAPERLESS_EXPORT_DIR: /data/export
              PAPERLESS_MEDIA_ROOT: /data/media
              PAPERLESS_OCR_LANGUAGES: nld
              PAPERLESS_OCR_LANGUAGE: nld
              PAPERLESS_REDIS: redis://paperless-redis:6379
              USERMAP_UID: "568"
              USERMAP_GID: "568"
            envFrom:
              - secretRef:
                  name: paperless-secret
            probes:
              liveness:
                enabled: true
              readiness:
                enabled: true
              startup:
                enabled: true
                spec:
                  failureThreshold: 30
                  periodSeconds: 5
            resources:
              requests:
                cpu: 25m
                memory: 768Mi
              limits:
                memory: 3Gi
      redis:
        pod:
          priorityClassName: database
        containers:
          main:
            image:
              repository: docker.io/valkey/valkey
              tag: "9.1.2@sha256:c123e3715db63d06d4ad6964884037aa0d5d4d703939b9929954112889708e1d"
            resources:
              requests:
                cpu: 5m
                memory: 32Mi
              limits:
                memory: 64Mi
    service:
      main:
        controller: main
        ports:
          http:
            port: 8000
      redis:
        controller: redis
        ports:
          http:
            port: 6379
    route:
      main:
        parentRefs:
          - name: envoy-internal
            namespace: network
        hostnames:
          - paperless.bo0tzz.me
        rules:
          - backendRefs:
              - identifier: main
                port: 8000
    persistence:
      data:
        existingClaim: paperless
        advancedMounts:
          main:
            main:
              - path: /data
      scan-inbox:
        existingClaim: scan-inbox
        advancedMounts:
          main:
            main:
              - path: /data/incoming/scans
