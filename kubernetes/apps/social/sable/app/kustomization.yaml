---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: social
resources:
  - ocirepository.yaml
  - helmrelease.yaml
configMapGenerator:
  - name: sable-config
    files:
      - config.json=./config.json
generatorOptions:
  disableNameSuffixHash: false
